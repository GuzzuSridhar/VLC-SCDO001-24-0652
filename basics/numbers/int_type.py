# integers are whole numbers without decimal places
# example
age = 25
print(type(age))
salary = 4000
count = -1

#  in python number types (int and float) are signed
# signed integer means the variable can hold positive and negative values
# unsigned integer means the variable can hold only positive  values
